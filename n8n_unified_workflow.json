{
  "name": "finaCia - Professional Business API",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "GET",
        "path": "finacia-unified-data",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "u1-webhook",
      "name": "Webhook (Business API)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [0, 300]
    },
    {
      "parameters": {
        "operation": "getAll",
        "documentId": {
          "__rl": true,
          "value": "ID_HOJA_NEGOCIO",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "Hoja 1",
          "mode": "name"
        },
        "options": {}
      },
      "id": "u2-sheets-business",
      "name": "Leer Negocio",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4,
      "position": [250, 300]
    },
    {
      "parameters": {
        "jsCode": "/* NODES: PROCESAMIENTO PROFESIONAL DE NEGOCIO */\nconst inputData = $input.all();\nconst business = { transactions: [] };\n\nfunction cleanCurrency(val) {\n  if (val === undefined || val === null || val === \"\") return 0;\n  try {\n    let s = String(val).replace(/[€*%\\s\\(\\)]/g, '').trim();\n    if (s === \"\" || s === \"-\") return 0;\n    if (s.includes(',') && s.includes('.')) s = s.replace(/\\./g, '').replace(',', '.');\n    else if (s.includes(',')) s = s.replace(',', '.');\n    else if (s.includes('.')) {\n      const parts = s.split('.');\n      if (parts[parts.length-1].length === 3) s = s.replace(/\\./g, '');\n    }\n    return parseFloat(s) || 0;\n  } catch (e) { return 0; }\n}\n\nfor (const item of inputData) {\n  const json = item.json; if (!json) continue;\n  if (json.Monto !== undefined) {\n    const amount = cleanCurrency(json.Monto);\n    let type = String(json.Tipo || '').trim().toUpperCase();\n    let concept = String(json.Concepto || '').trim().toUpperCase();\n    \n    if (concept.includes('ADS') || concept.includes('PUBLICIDAD') || type.includes('PUBLICIDAD')) {\n       type = 'Publicidad';\n    } else if (type.includes('COMPRA')) {\n       type = 'Compra';\n    } else {\n       type = 'Venta';\n    }\n    \n    business.transactions.push({\n      date: json.Fecha || '-',\n      item: json.Concepto || '-',\n      type: type,\n      amount: amount\n    });\n  }\n}\n\nreturn [{ json: { business } }];\n"
      },
      "id": "u4-code",
      "name": "Procesador de Datos",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [500, 300]
    }
  ],
  "connections": {
    "Webhook (Business API)": {
      "main": [
        [{"node": "Leer Negocio", "type": "main", "index": 0}]
      ]
    },
    "Leer Negocio": {
      "main": [
        [{"node": "Procesador de Datos", "type": "main", "index": 0}]
      ]
    }
  }
}
